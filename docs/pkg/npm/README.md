# icpay-bucket (npm) — full reference

See also the package README shipped on npm (`pkg/npm/icpay-bucket/README.md`).

**Install:** `npm install icpay-bucket@1.1.0`

The npm client wraps all 43 canister bucket methods. See the npm README for the complete method table grouped by: Buckets, Upload, Read, Write, Bulk, API keys.

## Test

```bash
BUCKET_API_KEY=icp_cloud_… bash pkg/testing/npm/run.sh
```

## Publish

See `pkg/readme` section 4.
