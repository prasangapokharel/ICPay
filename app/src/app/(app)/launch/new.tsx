import { Screen } from '@/components/layout/screen'
import { LaunchCreateScreen } from '@/features/launch/launch-create-screen'

export default function LaunchNewRoute() {
  return (
    <Screen>
      <LaunchCreateScreen />
    </Screen>
  )
}
