# ICPay

Claude Code auto-loads this file. It does not auto-load `AGENTS.md` at any path,
so the map is imported rather than duplicated — one source of truth, and other
agent tools that do read `AGENTS.md` see the same content.

@AGENTS.md
